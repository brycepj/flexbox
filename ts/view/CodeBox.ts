module flexbox {

    export module view {

        export class CodeBox {
            
            index:number;
            model:any;
            
            
            constructor(model,index) {
                
                this.model = model;
                this.index = index;
                

                //flexbox within a flexbox?



            }
        }

    }

}
